# Pinned to the versions the pipeline was developed and verified against.
# Requires Python >= 3.12 (xgboost 3.3.0 does not publish wheels for 3.11).
# Verified on Python 3.13; the Dockerfile and CI both use 3.13.
pandas==2.3.3
numpy==2.3.5
scikit-learn==1.7.2
scipy==1.16.3
joblib==1.5.2
xgboost==3.3.0
mlflow==3.14.0
pandera==0.32.1
evidently==0.7.21
pytest==8.4.2
matplotlib==3.10.6
pyyaml==6.0.3
# Data/artefact versioning (optional for running the pipeline; used by dvc.yaml).
dvc==3.67.1
# Required to read the OpenML Parquet source in src.fetch_data. MLflow 3.14.0
# requires pyarrow<25, so 21.0.0 is the compatible verified direct pin.
pyarrow==21.0.0
shap==0.52.0